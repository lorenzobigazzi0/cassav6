# Migration status (REV2)

Aggiornare con evidenze verificabili. Stati ammessi: `TODO`, `IN_PROGRESS`,
`BLOCKED`, `DONE`.

**Non usare percentuali stimate come sostituto dei gate.** Un task e `DONE`
quando la sua Definition of Done in `tasks/MIGRATION_TASKS.csv` e soddisfatta e
c'e un riferimento a un'evidenza (commit, test, report archiviato), non quando
"sembra fatto".

## Decisioni bloccanti

Nessuna fase oltre P2b parte con la sua decisione bloccante aperta.

| Decisione | Blocca | Stato | Evidenza |
|---|---|---|---|
| HW-01 storage e coesistenza motori | P1 | IN_PROGRESS | Eccezione DEV formalizzata in `DOCUMENTAZIONE/HW01_SD_DEVELOPMENT_DECISION_20260831.md`: sviluppo consentito su microSD; produzione e cutover restano bloccati fino alla rivalutazione sullo storage definitivo |
| SEQ-01 sequenziamento Commerciale V2 | P4 | TODO | |
| BIZ-01 business date | P5 | TODO | |
| CONC-01 ambizione cambio concorrenza | P6b | TODO | |
| CASH-01 Glory e contanti | P7 | TODO | |
| FIS-01 fiscale | P8 | TODO | |
| RET-01 retention | P11 | DECISA | Finestre confermate il 2026-09-02 senza modifiche e tabelle ad alto volume partizionate alla creazione: `RET-01:APPROVED-2026-09-02` in `backend/db/postgresql/migrations/007_ret01_retention_approval.sql`, con `12_OPEN_DECISIONS.md`. Migration 007 **applicata e idempotente sul Raspberry DEV il 2026-09-02** dopo backup logico (sha256 `3f5ca421...`, restore di prova 13 righe in 129 ms): registro a 007, 8 policy approvate, 0 abilitate, 0 ancora `TODO`, 5 protette intatte, runtime `SELECT=true`/`UPDATE=false`, `audit.events` ancora partizionata con 16 partizioni, health 200/302 e nessun database temporaneo residuo. Evidenza: `SORGENTE_SISTEMA/cassa-frontend/reports/postgresql-migration/mig026/raspberry-dev-sd-ret01-approval-20260902.json`. L'attivazione resta una UPDATE esplicita per policy, ancora da fare |
| FMT-01 formato importi legacy | P12 | TODO | |
| ROL-01 rollback dopo cutover | P14 | TODO | |

## Fasi

| Fase | Stato | Gate superato | Evidenza |
|---|---|---|---|
| P0 Baseline e inventario | IN_PROGRESS | No | MIG-001 e MIG-003 chiuse; MIG-002 riaperta con fixture v2 non vuota e MIG-000 resta incompleta. Vedere `DOCUMENTAZIONE/P2B_PREREQUISITES_20260901.md` |
| P1 Infrastruttura PostgreSQL | IN_PROGRESS | DEV_ONLY | Driver, runner, backup/restore e PITR DEV verificati sul Raspberry; HW-01-PROD, backup su device indipendente e drill con dataset reale restano aperti; nessun cutover e autorizzato |
| P2 Foundation persistence | IN_PROGRESS | DEV_ONLY | MIG-020..MIG-025 completate; migration 006 e 007 applicate e idempotenti sul Raspberry: audit partizionato con 16 partizioni, default e ID globale; RET-01 decisa e approvata il 2026-09-02, le 13 policy restano tutte disabilitate |
| P2b Decomposizione server.js | IN_PROGRESS | Pilot only | **Slice identity concluso**: 7 route su 7 a zero accessi globali, da 7 `readDb` e 11 `writeDb` a **0 e 0**; ne `backend/auth/auth.handlers.js` ne `backend/users/users.handlers.js` contengono piu un accesso all'app-state. **Chiusi a 0/0 anche i domini `configuration`, `catalog`, `commerce`, `audit`, `messaging`, `app_meta` e `fiscal`**: otto domini su tredici, 63 route sulle 198. **MIG-030 chiusa**: inventario dei confini esteso a tutte le 198 route con dominio, marcatura cross-domain, contenitori legacy e collezioni app-state. **MIG-030 chiusa e MIG-031 avviata**: `server.js` da 38.831 a 31.371 righe; handler conclusi, soglia 25.000 valutata e non perseguita; MIG-032/033 valgono su tutti i domini. Vedere `DOCUMENTAZIONE/P2B_IDENTITY_PILOT_20260901.md` |
| P3 Identity e configurazione | TODO | | |
| P4 Catalogo, menu, commerciale | TODO | | |
| P4b Coupon/voucher/benefit | TODO | | |
| P5 Sale, tavoli, sessioni, prenotazioni | TODO | | |
| P6 Ordini | TODO | | |
| P6b Modello di concorrenza | TODO | | |
| P7 Pagamenti, provider, contanti | TODO | | |
| P8 Fiscale e stampa | TODO | | |
| P9 Realtime e cache | TODO | | |
| P10 Domini secondari | TODO | | |
| P11 Report e analytics | TODO | | |
| P12 Import storico e verifica | TODO | | |
| P13 Hardening e load test | TODO | | |
| P14 Cutover | TODO | | |
| P15 Decommission | TODO | | |

## Task

| Task | Stato | Commit | Test/evidenza | Note |
|---|---|---|---|---|
| MIG-000 | IN_PROGRESS | workspace senza Git | `SORGENTE_SISTEMA/cassa-frontend/reports/postgresql-migration/p2b/baseline-known-failures-20260901.json`; `SORGENTE_SISTEMA/cassa-frontend/scripts/postgresql-migration/p2b-baseline-allowlist.json` | Baseline non verde ma ora esatta e falsificabile: Cassa 72/92 con 20 rossi noti, Mobile 639/642 con 3; gate blocca nuovi rossi, failure mancanti e deriva conteggi; performance Raspberry mancante |
| MIG-001 | DONE | workspace senza Git | `SORGENTE_SISTEMA/cassa-frontend/scripts/postgresql-migration/p0-artifacts.mjs`; `legacy-storage-inventory.csv` | 228 `readDb` runtime in 35 file, 91 `writeDb` runtime in 20 file |
| MIG-002 | IN_PROGRESS | workspace senza Git | `SORGENTE_SISTEMA/cassa-frontend/reports/postgresql-migration/p0/golden-dataset.json`; `golden-dataset.sha256`; `DOCUMENTAZIONE/P2B_PREREQUISITES_20260901.md` | Fixture v2 SHA-256 `ac184311c9c96bb8b2d02ff23f71f284afaebfca94437e6435fd501b24edfa01`: listini ereditati/overlap/overnight, 5 casi prezzo, identity/sessioni, allergeni distinti, ordine scontato, split payment e benefit parziale; resta aperta finche importer/equivalence gate la consumano |
| MIG-003 | DONE | workspace senza Git | `SORGENTE_SISTEMA/cassa-frontend/reports/postgresql-migration/mig003/raspberry-production-20260831.json`; `DOCUMENTAZIONE/MIG003_RASPBERRY_BASELINE_20260831.md` | 61 campioni/301,4 s; Node RSS max 59.916.288 B, MariaDB 205.832.192 B, 49,4 C, throttling `0x0` |
| MIG-010 | IN_PROGRESS | workspace senza Git | `DOCUMENTAZIONE/HW01_SD_DEVELOPMENT_DECISION_20260831.md`; `DOCUMENTAZIONE/MIG010_MIG011_POSTGRESQL_DEV_SD_20260831.md` | PostgreSQL 17 DEV predisposto su microSD; `pg_test_fsync`/`fio` sullo storage definitivo e HW-01-PROD restano aperti |
| MIG-011 | DONE | workspace senza Git | `SORGENTE_SISTEMA/cassa-frontend/backend/tests/postgresql-runtime.test.mjs`; `DOCUMENTAZIONE/MIG010_MIG011_POSTGRESQL_DEV_SD_20260831.md` | Driver `pg`, pool lazy opt-in, health, gauge pool e queue wait verificati; smoke Raspberry `ready` in 62,22 ms |
| MIG-012 | DONE | workspace senza Git | `SORGENTE_SISTEMA/cassa-frontend/backend/tests/postgresql-migrations.test.mjs`; `DOCUMENTAZIONE/MIG012_POSTGRESQL_MIGRATION_RUNNER_20260831.md` | Runner idempotente con advisory lock, transazione, `schema_migrations` e checksum; smoke Raspberry applicato due volte, drift bloccato e rollback verificato |
| MIG-013 | IN_PROGRESS | workspace senza Git | `SORGENTE_SISTEMA/cassa-frontend/reports/postgresql-migration/mig013/raspberry-dev-sd-20260831.json`; `DOCUMENTAZIONE/MIG013_POSTGRESQL_BACKUP_PITR_DEV_SD_20260831.md` | Restore 21 ms e PITR 6.065 ms riguardano soltanto `temporary_mig013_probe` vuoto e non stimano la manutenzione; dataset reale, capacity gate e backup su storage indipendente restano aperti |
| MIG-020 | DONE | workspace senza Git | `SORGENTE_SISTEMA/cassa-frontend/backend/tests/postgresql-foundation-migration.test.mjs`; `SORGENTE_SISTEMA/cassa-frontend/reports/postgresql-migration/mig020/raspberry-dev-sd-20260831.json`; `DOCUMENTAZIONE/MIG020_POSTGRESQL_FOUNDATION_20260831.md` | Migration `001_foundation` applicata e idempotente; 5 tabelle, grant least-privilege e restore post-migration verificati sul Raspberry |
| MIG-021 | DONE | workspace senza Git | `SORGENTE_SISTEMA/cassa-frontend/backend/tests/postgresql-transactions.test.mjs`; `SORGENTE_SISTEMA/cassa-frontend/reports/postgresql-migration/mig021/raspberry-dev-sd-20260831.json`; `DOCUMENTAZIONE/MIG021_POSTGRESQL_TRANSACTION_HELPER_20260831.md` | COMMIT/ROLLBACK standardizzati; retry limitato a `40001`/`40P01`; concorrenza `SERIALIZABLE` reale con 1 retry misurato e valore finale coerente |
| MIG-022 | DONE | workspace senza Git | `SORGENTE_SISTEMA/cassa-frontend/backend/tests/repository-boundary.test.mjs`; `SORGENTE_SISTEMA/cassa-frontend/reports/postgresql-migration/mig022/repository-boundary-20260831.json`; `DOCUMENTAZIONE/MIG022_REPOSITORY_BOUNDARY_20260831.md` | Contratto repository strutturale; gate universale integrato nella release; 334 file runtime e 47 handler analizzati con 0 violazioni SQL fuori dagli owner persistence |
| MIG-023 | DONE | workspace senza Git | `SORGENTE_SISTEMA/cassa-frontend/backend/tests/postgresql-event-outbox.test.mjs`; `SORGENTE_SISTEMA/cassa-frontend/reports/postgresql-migration/mig023/raspberry-dev-sd-20260831.json`; `DOCUMENTAZIONE/MIG023_POSTGRESQL_EVENT_OUTBOX_20260831.md` | Claim concorrente reale 6+6 con 0 duplicati; lease scaduto ripreso; stale owner rifiutato; due consegne con chiave stabile hanno prodotto un solo side effect; migration 002 applicata e idempotente sul DEV |
| MIG-024 | DONE | workspace senza Git | `SORGENTE_SISTEMA/cassa-frontend/backend/tests/postgresql-audit-events.test.mjs`; `SORGENTE_SISTEMA/cassa-frontend/reports/postgresql-migration/mig024/raspberry-dev-sd-20260831.json`; `DOCUMENTAZIONE/MIG024_POSTGRESQL_APPEND_ONLY_AUDIT_20260831.md` | Audit e business write nello stesso commit; rollback atomico reale; runtime bloccato con `42501` e owner con `55000` su update/delete/truncate; migration 003 applicata e idempotente sul DEV |
| MIG-025 | DONE | workspace senza Git | `SORGENTE_SISTEMA/cassa-frontend/backend/tests/postgresql-idempotency-keys.test.mjs`; `SORGENTE_SISTEMA/cassa-frontend/reports/postgresql-migration/mig025/raspberry-dev-sd-20260831.json`; `DOCUMENTAZIONE/MIG025_POSTGRESQL_IDEMPOTENCY_STORE_20260831.md` | Concorrenza reale 8 chiamanti: 1 esecuzione e 7 replay identici; conflict con hash diverso senza side effect; rollback recuperabile; 0 processing orfani; migration 004 applicata e idempotente sul DEV |
| MIG-026 | IN_PROGRESS | workspace senza Git | `SORGENTE_SISTEMA/cassa-frontend/reports/postgresql-migration/mig026/raspberry-dev-sd-partitioned-20260901.json`; `SORGENTE_SISTEMA/cassa-frontend/backend/db/postgresql/migrations/006_audit_events_partitioned_retention.sql`; `DOCUMENTAZIONE/MIG026_POSTGRESQL_RETENTION_CONTROL_PLANE_20260831.md` | Migration 006 applicata e idempotente sul Raspberry/microSD dopo backup+restore: relkind `p`, 16 partizioni, default e registro ID globale, 0 eventi, health 200/200. RET-01 decisa e **migration 007 applicata al DEV il 2026-09-02**, due volte, con backup e restore di prova: 8 finestre approvate e tutte disabilitate, 5 protette intatte, privilegi runtime invariati. Evidenza in `reports/postgresql-migration/mig026/raspberry-dev-sd-ret01-approval-20260902.json`. MIG-026 resta aperta soltanto perche nessuna policy e attiva: l'abilitazione richiede uno scheduler e va fatta una policy alla volta, quando le tabelle esistono |
| MIG-030 | DONE | workspace senza Git | `SORGENTE_SISTEMA/cassa-frontend/reports/postgresql-migration/p2b/server-route-boundaries.csv`; `route-boundaries-baseline-20260902.json`; `SORGENTE_SISTEMA/cassa-frontend/scripts/postgresql-migration/route-domain-map.mjs` | Inventario esteso da 7 a **198 route su 198**, 193 handlerKey, tutte con dominio preso dai 14 schemi di `02_TARGET_ARCHITECTURE.md` e 175 con marcatura cross-domain. Il CSV riporta anche le collezioni app-state lette e scritte per route: la dichiarazione in `route-domain-map.mjs` e autoritativa, l'analizzatore statico `route-source-index.mjs` fa da rete e il gate fallisce se deduce un accesso non dichiarato. 197 route risolte staticamente su 198 (`health` non ha accessi e porta la nota), 134 mutative, 162 che scrivono app-state. **Revisione del 2026-09-02**: il confronto fra dominio dichiarato e collezioni realmente toccate ha trovato due difetti nell'analizzatore, entrambi corretti - seguiva i sanitizer, che non ricevono l'app-state e quindi non possono leggerlo, e consumava gli argomenti perdendo le chiamate annidate. Ha inoltre mostrato che `crossDomain` era saturo: 175 route su 198 marcate cross-domain perche `posSettings` (130 route) e `integration` (63) nel legacy sono contenitori condivisi dove convivono impostazioni, sale, tavoli, ordini e prenotazioni, mentre le collezioni omonime sono quasi vuote (`orders` 2 route, `rooms`/`tables`/`reservations` 1 ciascuna). Ora sono due misure distinte: **89 route con cross-domain reale** e **124 che attraversano un contenitore legacy** - questa seconda e la misura del lavoro che attende MIG-032/033. Un test impedisce alla colonna di tornare satura. Gate `test:migration:pg:p2b-routes` 8/8, gate identity 3/3, baseline `comparison.ok: true`. Nessun codice runtime modificato. Assegnazione dei domini confermata: il controllo incrociato non ha trovato alcun dominio sbagliato e i casi di confine sono stati decisi esplicitamente - `room-move` in `reservations` come vuole la definizione dello schema target ("prenotazioni e cambi tavolo/sala"), `reports.*` attribuito al dominio del dato riportato visto che fra i 14 schemi non esiste "reporting" (con `reports.sales` corretto da `payments` a `sales`), `settings.assignBill` in `sales`, `vat`/`ip`/`city` in `app_meta` perche sono lookup esterni senza app-state. **DoD soddisfatta**: CSV completo, ogni route con dominio e marcatura cross-domain  **Correzione del 2026-09-04: una route sfuggiva all'inventario, e non era quella che credevamo.** Lo stato diceva "197 route risolte staticamente su 198 (`health` non ha accessi e porta la nota)". La seconda meta era falsa. `indexHandlerExpressions` riconosce le voci del dispatch solo con la chiave **fra virgolette**; nella mappa di `status.handlers.js` la voce di `health` era scritta `health: handleHealth`, senza, unica fra tutte. Non veniva indicizzata, `resolve("health")` rispondeva "handlerKey assente dal dispatch", e siccome il gate fallisce solo quando **deduce** un accesso non dichiarato, per una route irrisolvibile non poteva dedurre nulla: la dichiarazione `reads: []` non e mai stata verificata. **Ed era falsa**: `handleHealth` legge `db.meta` come fallback quando lo snapshot di salute non porta `settingsVersion`. Corretti sia l'analizzatore -- ora accetta anche le chiavi non quotate, purche identificatori con valore `handleXxx`, che e la forma di ogni voce di dispatch -- sia la chiave nel sorgente, quotata come le altre. La prima correzione e quella che conta: senza, la correttezza dell'inventario dipende da come qualcuno ha scritto una chiave. Effetto: `unresolvedRouteCount` da 1 a **0**, tutte le 198 route risolte, e `app_meta` passa da 4 a **5 `readDb`** residui perche `health` ora e contato. La dichiarazione e stata corretta a `reads: ["meta"]` con la motivazione in `note`, come vuole la regola per chi dichiara piu di quanto l'analisi deduca. Gate 8/8, process-topology + route-policy + relational-persistence 168/168, continuity 69/69, architetturale 198 route e 0 violazioni |
| MIG-031 | IN_PROGRESS | workspace senza Git | 19 moduli sotto `SORGENTE_SISTEMA/cassa-frontend/backend/modules/`; `npm run gate:architecture-security` | **Diciannove blocchi estratti**, tutti spostamenti verbatim con dipendenze iniettate dal composition root. `server.js` da **38.831 a 31.371** righe (**-7.460, -19%**). In `server.js` restano 10 handler di route per sole **284 righe** complessive, tutti isolati: estrarli costerebbe dieci cicli per un guadagno netto di ~130 righe, quindi **il lavoro sugli handler e considerato concluso**. Suite invariata dopo ogni blocco: continuity 69/69, auth session 25/25, payment weird cases 17/17, order delivery 4/4, notification native session 3/3, gate identity 3/3, MIG-030 8/8, baseline `comparison.ok: true`, architetturale 198 route e 0 violazioni. **La soglia di 25.000 righe della DoD e stata valutata e non perseguita**, per un limite strutturale misurato: gli helper rimasti sono funzioni hoisted referenziate a meta file, e spostarle in una factory le trasforma in `const` che finiscono in temporal dead zone. Sul blocco fiscale la finestra di collocazione e risultata **vuota**: primo uso a riga 12.387, ma `readDb` e definita a 16.466 e `findPosFiscalReceiptByPaymentId` a 21.340. Poiche `readDb`/`writeDb` stanno a ~16.500 e servono a quasi ogni helper, ogni blocco referenziato prima di quella riga ha lo stesso problema. **Questo non blocca MIG-032/033**: il pilot identity le ha portate a 0/0 su sette route con `server.js` ancora a 38.800 righe, quindi la dipendenza formale MIG-032 -> MIG-031 e una euristica, non un vincolo tecnico. La strada praticabile, se si vorra riprendere: facciata hoisted con risoluzione pigra (un guscio `function` in `server.js` che dereferenzia il modulo alla chiamata), immune alla TDZ per costruzione, al costo di ~3 righe per funzione esportata  **Trentasette dipendenze perse trovate e ricablate il 2026-09-03**: i moduli estratti usavano nomi che il composition root non iniettava. Sono guasti silenziosi -- `node --check` passa, il server si avvia, e la route restituisce 500 solo quando quella riga viene eseguita, quindi in un ramo raro possono restare invisibili per settimane. Distribuzione: `payments/pay-ticket` 10, `table-room-move/room-move` 6, `payments/assign-bill` 4, `pos-rooms/room-change` 4, dodici fra i sette moduli `integration` (`order-sync`, `bar-charge`, `order-comp`, `order-correction`, `order-transfer-request`, `print`, `table-move`) e `notifications/waiter-pause` 1 (`buildAuditActor`). Verifica attuale **0**. Vedere P2B-DEP-01 |
| P2B-CONF-01 | DONE | workspace senza Git | `SORGENTE_SISTEMA/cassa-frontend/backend/modules/settings/settings-read-model.js` e `settings-write-model.js`; `modules/radio/radio-read-model.js` e `radio-write-model.js`; `modules/status/configuration-read-model.js` | Dominio `configuration` a **0 `readDb` e 0 `writeDb`** diretti su tutte e 19 le route, da 19/10. Cinque modelli nuovi per 824 righe; `settings.handlers.js` da 657 a 313 righe e `radio.handlers.js` da 209 a 128. `settings.saveOrderWorkflow` e uscita anche da `server.js` portandosi dietro **verbatim** l'effetto cross-domain sugli ordini (auto-consegna, audit `order.auto_delivered_by_workflow_setting`, sync finanziario tavoli). Conservate le tre differenze delle route di stato: `allowMigrations: false`, nessuna validazione sessione e lettura da `menuSettingsRepository` con fallback. Suite invariata: continuity 69/69, auth session 25/25, order delivery 4/4, radio 6/6, payment weird cases 17/17, gate route 8/8, gate identity 3/3, baseline `comparison.ok: true`, architetturale 198 route e 0 violazioni. **Con identity, P3 ha entrambe le sue meta a 0/0** |
| P2B-CAT-01 | DONE | workspace senza Git | `SORGENTE_SISTEMA/cassa-frontend/backend/modules/menu/menu-read-model.js` e `menu-write-model.js`; `backend/tests/catalog-commerce.e2e.test.mjs` | Dominio `catalog` a **0 `readDb` e 0 `writeDb`** diretti su tutte e 5 le route, da 5/1. Due modelli nuovi per 555 righe; `menu.handlers.js` da 701 a 372 righe (tre handler di tre righe) e `integration-menu.handlers.js` da 153 a 44. `integration.menuTopSold` e uscita anche da `server.js`: il handler resta a estrarre `days` e `limit` dalla query, il resto passa al reader. Due cose conservate di proposito: `settings.menu` resta **una sola funzione a due rami**, perche senza `items` nel payload la route si comporta da lettura e ritorna presto -- spezzarla avrebbe inventato una route che non esiste; e `readIntegrationMenuView` dichiara *come* va inviata la risposta (`json` per il corpo gia serializzato dalla cache veloce, `payload` per l'oggetto), perche la scelta fra `sendJsonString` e `sendJson` e l'unica cosa che resta al handler. Suite invariata: menu settings e2e 3/3, menu domain 5/5, default drink premium 4/4, relational menu settings 12/12, continuity 69/69, gate route 8/8, gate identity 3/3, baseline `comparison.ok: true`, architetturale 198 route e 0 violazioni |
| P2B-COM-01 | DONE | workspace senza Git | `SORGENTE_SISTEMA/cassa-frontend/backend/modules/commercial-benefits/commercial-benefits-read-model.js` e `commercial-benefits-write-model.js`; `backend/tests/catalog-commerce.e2e.test.mjs` | Dominio `commerce` a **0 `readDb` e 0 `writeDb`** diretti su tutte e 7 le route, da 8/6. Due modelli nuovi per 479 righe; `commercial-benefits.handlers.js` da 970 a 602 righe, con i sette handler ridotti a tre righe e gli aiutanti puri esportati per i modelli. Il contesto di autenticazione gia risolto dal middleware (`req.__authContext`) arriva ai modelli come secondo argomento, con lo stesso fallback su `validateSessionContext` di prima; lo stato 201 di `createCampaign` resta nel handler, perche appartiene alla route e non alla regola. **La doppia lettura di `printCoupon` e stata spostata intatta**: legge, accoda il job di stampa -- un effetto esterno -- e solo dopo rilegge in `latestDb` per non sovrascrivere cio che l'accodamento ha nel frattempo scritto. Collassarla avrebbe fatto sparire il job senza che nulla fallisse, percio la nuova suite e2e verifica proprio che il job sopravviva alla scrittura successiva. `commercial-benefits` 12/12 dopo il ricablaggio della sua factory (asserzioni intatte), collaudo `catalog-commerce` 2/2, continuity 69/69, gate route 8/8, baseline `comparison.ok: true`, architetturale 0 violazioni |
| P2B-ID-01 | DONE | workspace senza Git | `SORGENTE_SISTEMA/cassa-frontend/reports/postgresql-migration/p2b/identity-route-boundaries.csv`; `identity-pilot-baseline-20260901.json`; `DOCUMENTAZIONE/P2B_IDENTITY_PILOT_20260901.md` | Inventario/gate identity verde: 7 route, 2 handler, P2b.2 gia presente nello slice; chiuse e verificate `users.list` (`backend/users/users-list-read-model.js`), `auth.changePin` (`backend/auth/change-pin-write-model.js`), `auth.selectWorkstation` (`backend/auth/select-workstation-write-model.js`) `auth.sessionStatus` (`backend/auth/session-status-write-model.js`, retry serializzato e fast/fallback write invariati) `auth.login` (`backend/auth/login-write-model.js`, con `backend/auth/volatile-session-cache.js` completata da `forgetVolatileSessions`), `auth.logout` (`backend/auth/logout-write-model.js`, tre rami di scrittura e publish di handoff invariati) e `users.save` (`backend/users/users-save-write-model.js`, invarianti di autotutela, revoca sessioni attesa e `writeDb` senza metrica invariati). **Da 7/11 a 0/0**: sette read/write model sono gli unici owner dell'app-state del dominio. `backend/auth/auth.handlers.js` e sceso da 819 righe (baseline 2026-08-31) a 188 con 12 dipendenze iniettate, `backend/users/users.handlers.js` da 306 a 25 con 4; nessun cambio di comportamento o database. Test nuovi `auth-session-status-handler` 5/5, `auth-login-handler` 17/17, `auth-logout-handler` 12/12 e `user-app-users-handler` 15/15; auth session 25/25, continuity 69/69, redis cache 6/6, gate identity 3/3, gate architetturale 0 violazioni e baseline esatta verde (`comparison.ok: true`) |

| P2B-DEP-01 | DONE | workspace senza Git | analizzatore di variabili libere su AST (`@babel/parser`); `DOCUMENTAZIONE/HANDOVER_IA_STATO_20260903.md` sezione 6 | **Controllo permanente da eseguire dopo ogni estrazione di MIG-031/032/033.** Le espressioni regolari hanno dato un risultato sbagliato **tre volte** su questo codice -- costanti dichiarate in gruppo, parametri con valore di default, arrow senza parentesi, destrutturazioni annidate -- e ogni volta il conteggio sembrava tornare. Il metodo che funziona costruisce la catena di scope reale (modulo, funzione, blocco, catch) con un primo passaggio di raccolta delle dichiarazioni, cosi le funzioni hoisted e le mutue ricorsioni non risultano libere; risolve ogni `Identifier` contro quella catena saltando le chiavi non referenzianti (`MemberExpression.property` non calcolata, `ObjectProperty.key` non calcolata, `VariableDeclarator.id`); sottrae le globali; e **interseca il residuo con i nomi dichiarati al livello superiore di `server.js`**. E' quest'ultimo passaggio a rendere il risultato utilizzabile: il walker produce falsi positivi, ma un nome che il modulo non risolve **e** che esiste nello scope di `server.js` e per definizione una dipendenza rimasta indietro. Un numero diverso da zero e il conto delle route che andranno in 500. Il deploy sul Raspberry DEV e il giro POS completo sul palmare (comanda creata, consegnata, incassata in contanti, pagamento in MariaDB) hanno esercitato proprio le route ricablate  **Correzione del 2026-09-04**: il filtro descritto sopra -- intersecare con i nomi di `server.js` -- e troppo stretto e ha lasciato passare un guasto reale. `settings-write-model.js` usava `findRoomTableExpansionViolations`, che vive in `settings.handlers.js` e **non** in `server.js`: l'intersezione l'ha scartata come rumore e l'analizzatore ha dichiarato zero mentre `/api/settings/pos/areas/save` rispondeva **500** (`findRoomTableExpansionViolations is not defined`), con tre test di `settings-room-table-policy.e2e` rossi. L'intersezione va fatta con l'unione dei nomi esportati da **tutti** i moduli del backend, non con il solo composition root. **Ed esiste una seconda modalita di guasto, che quell'analizzatore non puo vedere per costruzione**: una dipendenza *dichiarata* fra i parametri destrutturati della factory e mai passata dal composition root non e un `ReferenceError` -- il nome e definito e vale `undefined` -- e si manifesta solo alla chiamata, come `x is not a function`. Serve un secondo controllo che confronti i parametri dichiarati da ogni `createXxx({...})` con le chiavi effettivamente passate in `server.js`: `scratchpad/dipendenze-non-passate.mjs`, 62 factory esaminate. Ha trovato `writeUserPaymentPreferences`, dichiarata come parametro e mai passata, che per giunta **ombreggiava** l'import omonimo gia presente nel modulo: dentro la factory valeva `undefined`. Entrambi i controlli vanno eseguiti dopo ogni estrazione; oggi tornano 0 e 0 |

| P2B-AUD-01 | DONE | workspace senza Git | `SORGENTE_SISTEMA/cassa-frontend/backend/modules/audit/audit-read-model.js` e `audit-write-model.js`; `backend/tests/audit-domain.e2e.test.mjs` (`npm run test:audit-domain`) | Dominio `audit` a **0 `readDb` e 0 `writeDb`** diretti su entrambe le route, da 2/1. Quinto dominio chiuso: **40 route sulle 198**. I due handler restano in `reports.handlers.js` -- e dove sono sempre stati, insieme ai report di vendita che appartengono ad altri domini -- ma si riducono a leggere il corpo, chiamare il modello e rispondere; le due funzioni arrivano dalla factory come le altre dipendenze. Il contesto gia risolto dal middleware arriva ai modelli come secondo argomento, con lo stesso fallback su `validateSessionContext` di `resolveReportsAuthContext`. La cosa non ovvia, e il motivo del test nuovo, e che **la cancellazione e logica e idempotente**: l'evento resta e si marcano `deletedAt`, `deletedBy` e `deleteReason`, ma il ramo `if (!currentEvent.deletedAt)` fa si che una seconda cancellazione non riscriva autore e data della prima. Senza quel ramo l'evento risulterebbe cancellato dall'ultimo che ci passa sopra e nessun altro test se ne accorgerebbe: la guardia e stata **rimossa apposta** per verificare che il test la veda, e il test e fallito con il messaggio atteso. Copre anche i 400 su id e motivo mancanti, il 404 su id inesistente, i 401 senza sessione, l'ordinamento dal piu recente, il limite, il filtro per azione, la sparizione dall'elenco normale con ricomparsa in `includeDeleted` e la traccia `security.admin_delete` con prima e dopo. `audit-domain` 2/2, `reports-auth-context` + `relational-shadow` + `route-policy-architecture` 199/199, continuity 69/69, gate route 8/8, gate architetturale 198 route e 0 violazioni, baseline `ok: true`. `server.js` da 31.471 a 31.502 righe: il cablaggio aggiunge al composition root cio che toglie ai moduli, ed e il compromesso previsto |

| P2B-MSG-01 | DONE | workspace senza Git | `SORGENTE_SISTEMA/cassa-frontend/backend/modules/notifications/messaging-model.js` | Dominio `messaging` a **0 `readDb` e 0 `writeDb`** diretti su tutte e 6 le route, da 3/0. Sesto dominio chiuso: **46 route sulle 198**. `notifications.handlers.js` scende da **768 a 84 righe**; il modello ne prende 766 piu la sessione della route SSE. Tre scelte da conoscere. **Primo**: le tre route stanno in un file solo e non divise fra read e write model, perche `notifications/pull` e dichiarata non mutativa ma **scrive davvero** -- tocca l'heartbeat delle sessioni e fa il flush delle chiamate differite -- e separarle direbbe una cosa falsa. **Secondo**: dove il handler faceva `sendJson(res, 200, X)` il modello fa `return X`, ma `acknowledgeNotification` e `resolveNotificationStreamSession` restituiscono una busta `{ stato, corpo }`, perche hanno rami che rispondono **401** con un `code` (`NOTIFICATION_SESSION_REVOKED`, `NOTIFICATION_IDENTITY_REQUIRED`) su cui il client fa affidamento e che un `HttpError` generico perderebbe; lo stato li dipende dai dati ed e deciso in fondo al flusso, quindi non poteva restare nel handler senza duplicare il controllo. E lo stesso criterio con cui `readIntegrationMenuView` dichiara come va inviata la risposta, e l'opposto del 201 di `createCampaign`, che invece appartiene alla route. **Terzo**: della route SSE `notifications/stream` e uscita **solo l'autenticazione**; il parsing della query e la consegna al runtime SSE restano nel composition root, perche sono route e trasporto, non dominio. **L'analizzatore allargato ha guadagnato subito il suo costo**: ha trovato `compareIntegrationNotifications` non importata nel modello nuovo -- la stessa classe di guasto che stamattina aveva prodotto tre 500 -- prima che uscisse dalla macchina di sviluppo. Verifica: notifiche 52/52 su otto file, realtime latency + continuity + audit 72/72, catalog-commerce + settings room/table + order delivery 10/10, gate route 8/8, gate architetturale 198 route e 0 violazioni, baseline `ok: true`. `server.js` da 31.501 a 31.522 righe |

| P2B-MON-01 | DONE | workspace senza Git | `SORGENTE_SISTEMA/cassa-frontend/backend/modules/status/monitor-projection.js`; `backend/tests/app-meta.e2e.test.mjs` (`npm run test:app-meta`) | **Precondizione per chiudere `app_meta`, non un avanzamento di dominio.** `buildMonitorOverview` e le 590 righe di proiezione che la circondano stavano dentro la chiusura di `createStatusHandlers`: finche restavano li, `monitor.overview` e `monitor.control` non potevano avere un modello che leggesse l'app-state, perche il modello si crea nel composition root e non poteva raggiungerla, e passargliela avrebbe creato una dipendenza circolare. Estratte in una factory `createMonitorProjection` con **27 funzioni** e **6 dipendenze**, creata una volta sola nel composition root; 9 funzioni tornano indietro a `status.handlers.js`, che scende da **1.784 a 1.203 righe**. Spostamento verbatim: il blocco non contiene ne accessi all'app-state ne risposte HTTP. **Prima dell'estrazione e stata scritta la rete di test che non c'era**: `monitor.control` era sfiorata da due sole menzioni in un altro file e `appState.get`/`sync` non avevano alcun e2e. Cinque casi, fra cui il confronto fra la proiezione servita da `/api/monitor/overview` e quella incorporata nella risposta di `/api/monitor/control`; verificata togliendo apposta la sezione `stations` dalla proiezione, con due casi che cadono e il messaggio atteso. **Prova che nulla si e spostato**: il CSV dei confini e **identico byte per byte** dopo la rigenerazione, `app_meta` resta a 5 `readDb` e 2 `writeDb`, `unresolvedRouteCount` resta 0. Verifica: app-meta 5/5, process-topology + route-policy + orders-payments-invariants 174/174, continuity + audit 71/71, gate route 8/8, gate architetturale 198 route e 0 violazioni, baseline `ok: true`, entrambi gli analizzatori di dipendenze a 0. `server.js` da 31.522 a 31.542 righe |

| P2B-META-01 | DONE | workspace senza Git | `SORGENTE_SISTEMA/cassa-frontend/backend/modules/app-state/app-state-model.js`; `backend/modules/status/app-meta-model.js`; `backend/tests/app-meta.e2e.test.mjs` | Dominio `app_meta` da **5 `readDb` e 2 `writeDb` a 1 e 1**: chiusi `appState.get`, `appState.sync`, `health` e `monitor.overview`. `app-state.handlers.js` scende da 113 a **42 righe** e `buildAppStatePayload` si sposta nel modello con le due route che la usavano. **Resta aperta `monitor.control`, e va detto perche**: il suo grappolo di funzioni locali e di **23 funzioni per 476 righe**, piu il corpo del handler, ed e un'estrazione grande quanto quella della proiezione (P2B-MON-01). Si sarebbe potuto soddisfare il conteggio passando `db` avanti e indietro fra handler e modello, ma sarebbe stata una foglia di fico: il criterio di P2b non e che il numero scenda, e che l'app-state abbia un owner. Due cose conservate: `appState.sync` scrive **solo se qualcosa e cambiato** -- il ramo `if (changed)` evita di spostare `meta.lastWriteAt` a ogni sincronizzazione -- e `resolveHealthSettingsVersion` porta nel modello **l'intera scelta**, non la sola lettura, perche spezzarla lascerebbe al handler il compito di sapere quando serve l'app-state. Verifica: app-meta 5/5, process-topology + route-policy + orders-payments-invariants + continuity 243/243, audit + catalog-commerce + notifiche + settings room/table 18/18, gate route 8/8, gate architetturale 198 route e 0 violazioni, baseline `ok: true`, CSV dei confini identico byte per byte, entrambi gli analizzatori a 0  **Chiusa il 2026-09-04 con l'estrazione di `monitor.control`**: il handler e il suo grappolo di **25 funzioni locali per 521 righe** sono in `backend/modules/status/monitor-control-model.js` (937 righe in tutto), e `status.handlers.js` scende da 1.784 a **300 righe**. La chiusura transitiva del grappolo e stata calcolata inseguendo i **riferimenti** e non solo le chiamate: `paymentIsActive` e passata a `.filter()` e con il solo criterio delle chiamate sarebbe rimasta indietro. Verificato prima di muovere che **nulla dell'intervallo 48-568 fosse riferito dal resto del file**. Due soli adattamenti al corpo: `payload` diventa un parametro e l'unica risposta viene restituita invece che inviata; la guardia di configurazione si divide fra handler (`readJsonBody`) e modello (`writeDb`). Nel blocco viaggia `removePaymentsForOrderIds`, che **non e usata da nessuno**: spostata com'era, perche cancellarla e una decisione a se e non parte di uno spostamento verbatim. **Dominio `app_meta` a 0/0: settimo chiuso, 58 route sulle 198.** Verifica: app-meta 5/5, process-topology + route-policy + orders-payments-invariants + continuity + audit 245/245, gate route 8/8, gate architetturale 198 route e 0 violazioni, baseline `ok: true`, CSV dei confini identico byte per byte, entrambi gli analizzatori a 0 |

| P2B-FIS-01 | DONE | workspace senza Git | `SORGENTE_SISTEMA/cassa-frontend/backend/modules/fiscal-pos/fiscal-model.js`; `backend/modules/payments/payments-fiscal-model.js` | Dominio `fiscal` a **0 `readDb` e 0 `writeDb`** su tutte e 5 le route, da 5/0. **Ottavo dominio chiuso: 63 route sulle 198.** Tre pezzi: `fiscal.command` -- unica funzione di `fiscal.handlers.js`, che scende da 167 a **15 righe** -- `reports.nonFiscalized`, che usciva da `server.js`, e le tre route fiscali sui movimenti di pagamento (**719 righe** piu sei funzioni del grappolo e la costante dei domini di scrittura). Tutte e otto le risposte erano 200, quindi i modelli restituiscono il corpo e i handler lo inviano: nessuna busta `{ stato, corpo }` come in `messaging`. **La rete di test non e stata scritta: esisteva gia** -- `payment-weird-cases.e2e` esercita emissione, annullamento e verifica contro un gateway finto -- e i numeri prima e dopo coincidono: 24/24 sulle suite fiscali e 47/47 su weird cases piu security. `resolvePaymentsReportReadDb` e stata spostata nel modello e **restituita ai handler**, perche era locale alla factory e la usano anche altre route: il composition root non avrebbe potuto passarla. Ripuntato `payment-movement-fiscal-actions.test.mjs`, che ritagliava le tre funzioni per offset dal sorgente: ora legge il modello, conserva **tutte** le asserzioni di contenuto e quelle d'ordine interne a ogni fetta, e abbandona la sola asserzione sull'ordine fra i quattro handler -- `reprint` e rimasta nei handler, quindi quell'ordine non esiste piu come fatto e non era un contratto ma il modo di tagliare. CSV dei confini identico byte per byte, `unresolvedRouteCount` 0, gate route 8/8, architetturale 198 route e 0 violazioni, baseline `ok: true`, continuity + app-meta + audit + catalog-commerce 79/79, static 174/174 |

Aggiungere una riga per task man mano che vengono presi in carico. L'elenco
completo con dipendenze, rischio e Definition of Done e in
`tasks/MIGRATION_TASKS.csv`.
